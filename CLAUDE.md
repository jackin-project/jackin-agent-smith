# CLAUDE.md

See [AGENTS.md](AGENTS.md) for shared agent instructions.

This is a **public Docker image** — an agent personality ("Agent Smith") built on `projectjackin/construct:trixie`. Never bake credentials into layers. Plugin additions should only come from `@claude-plugins-official` or `@jackin-marketplace` sources.

This repository uses `main` as its primary branch.
