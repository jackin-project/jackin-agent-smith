# CLAUDE.md

See [AGENTS.md](AGENTS.md) for shared agent instructions.

This is a **public Docker image** — a Claude Code agent image extending `projectjackin/construct:trixie` with the `code-review` and `feature-dev` plugins pre-configured for code-review-focused work. Never bake credentials into layers. Plugin additions should only come from `@claude-plugins-official` or `@jackin-marketplace` sources.

This repository uses `main` as its primary branch.
