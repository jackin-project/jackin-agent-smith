# Copyright 2026 Alexey Zhokhov
# SPDX-License-Identifier: Apache-2.0
name: reuse-compliance (public unmerged)
on:
  pull_request:
  workflow_dispatch:
    inputs:
      lanes:
        description: velnor (default) | github | both
        type: choice
        default: velnor
        options: [velnor, github, both]
permissions:
  contents: read
concurrency:
  group: reuse-compliance-public-unmerged-${{ github.ref }}
  cancel-in-progress: true

# Both lanes run the SAME steps with the SAME toolchain; only runs-on differs.
# pull_request routes to the GitHub lane (ubuntu-26.04) so PR checks never wait
# on the self-hosted fleet; workflow_dispatch keeps Velnor as the default lane.

jobs:
  reuse:
    name: REUSE (Velnor)
    if: ${{ github.event_name != 'workflow_dispatch' || inputs.lanes != 'github' }}
    runs-on: ${{ github.event_name == 'pull_request' && 'ubuntu-26.04' || fromJSON('["self-hosted","velnor-target-mvp"]') }}
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          persist-credentials: false
      - uses: jdx/mise-action@c2a87611a18de5b3828c5652fe268e992400cb5c # v4.3.0
        with:
          version: "2026.8.16"
          install_args: python uv pipx:reuse
          cache_save: false
          github_token: ${{ github.token }}
      - name: REUSE Compliance Check
        run: reuse lint

  reuse-github:
    name: REUSE (GitHub)
    if: ${{ github.event_name == 'workflow_dispatch' && inputs.lanes != 'velnor' }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - runner: ubuntu-26.04
    runs-on: ${{ matrix.runner }}
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          persist-credentials: false
      - uses: jdx/mise-action@c2a87611a18de5b3828c5652fe268e992400cb5c # v4.3.0
        with:
          version: "2026.8.16"
          install_args: python uv pipx:reuse
          cache_save: false
          github_token: ${{ github.token }}
      - name: REUSE Compliance Check
        run: reuse lint
